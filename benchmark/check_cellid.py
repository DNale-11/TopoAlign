import pandas as pd
df = pd.read_csv(r"F:\programme\nxy\Cell registration\benchmark\results\full_registration\A2-1\A2-B-1_to_A2-A-1\fixed_features.csv")
print("Columns:", df.columns.tolist())
print()
print("First 10 rows (key cols):")
print(df[["cell_id", "centroid_x", "centroid_y", "area"]].head(10).to_string())
print()
print(f"cell_id range: {df['cell_id'].min()} - {df['cell_id'].max()}")
print(f"Total cells: {len(df)}")
print(f"cell_id == row index? {(df['cell_id'].values == range(1, len(df)+1)).all()}")
